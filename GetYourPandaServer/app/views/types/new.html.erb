<h1> Create new types</h1>

<!-- form for new type -->
<%= form_for :type, url: types_path do |f| %>
	<p>
		<%= f.label :name%><br>
		<%= f.text_field :name%>
	</p>
	<p>
		<%= f.submit%>
	</p>
<% end %>
