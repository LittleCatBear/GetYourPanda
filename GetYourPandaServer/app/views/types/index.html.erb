<h1>Listing types</h1>

<%= link_to "New type", new_type_path %>

<table>
  <tr>
    <th>Title</th>
  </tr>
 
  <% @types.each do |type| %>
    <tr>
      <td><%= type.name %></td>
    </tr>
  <% end %>
</table>